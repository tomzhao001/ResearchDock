fastapi==0.115.6
uvicorn[standard]==0.32.1
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
pgvector==0.4.2
pydantic-settings==2.6.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# passlib 1.7.4 依赖 bcrypt.__about__；bcrypt 4.1+ 已移除
bcrypt==4.0.1
python-multipart==0.0.17
celery==5.6.2
redis==6.4.0
PyMuPDF==1.27.2.3
# Docling 负责 PDF 结构化；默认 OCR 为 rapidocr（需 onnxruntime）
# PyTorch 在 Dockerfile 中单独安装（CPU/GPU 可切换）
docling[rapidocr]==2.94.0
# EasyOCR 兼容兜底（会额外引入 easyocr + opencv-python-headless；恢复时注释上一行并启用下一行）
# docling[rapidocr,easyocr]==2.94.0
onnxruntime>=1.7.0,<2.0.0
symspellpy==6.9.0
langgraph==1.2.1
pytest==9.0.2
httpx[http2]==0.28.1
alembic==1.18.4
